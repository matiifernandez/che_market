<% content_for :page_title, "Editar: #{@product.name}" %>

<div class="mb-6">
  <a href="<%= admin_products_path %>" class="text-indigo-600 hover:text-indigo-800">
    ← Volver a productos
  </a>
</div>

<%= render "form" %>
