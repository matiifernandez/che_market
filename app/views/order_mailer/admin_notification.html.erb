<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
    <style>
      body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
      .container { max-width: 600px; margin: 0 auto; padding: 20px; }
      .header { background-color: #4F46E5; color: white; padding: 20px; text-align: center; border-radius: 8px 8px 0 0; }
      .content { background-color: #f9fafb; padding: 20px; border: 1px solid #e5e7eb; }
      .order-details { background-color: white; padding: 15px; border-radius: 8px; margin: 15px 0; }
      .product-row { padding: 10px 0; border-bottom: 1px solid #e5e7eb; }
      .total { font-size: 24px; font-weight: bold; color: #4F46E5; }
      .button { display: inline-block; background-color: #4F46E5; color: white; padding: 12px 24px; text-decoration: none; border-radius: 8px; margin-top: 15px; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>🧉 Nueva Orden!</h1>
      </div>

      <div class="content">
        <p>Tenés una nueva orden para procesar.</p>

        <div class="order-details">
          <h3>Orden #<%= @order.id %></h3>
          <p><strong>Fecha:</strong> <%= @order.created_at.strftime("%d/%m/%Y") %></p>
          <p><strong>Cliente:</strong> <%= @order.email %></p>

          <h4>Productos:</h4>
          <% @order.line_items.includes(:product).each do |item| %>
            <div class="product-row">
              <strong><%= item.product&.name || "Producto eliminado" %></strong><br>
              Cantidad: <%= item.quantity %> × <%= humanized_money_with_symbol(item.price) %> = <%= humanized_money_with_symbol(item.subtotal) %>
            </div>
          <% end %>

          <p class="total">Total: <%= humanized_money_with_symbol(@order.total) %></p>
        </div>

        <a href="<%= admin_order_url(@order) %>" class="button">Ver orden en el panel de Admin</a>
      </div>
    </div>
  </body>
</html>
